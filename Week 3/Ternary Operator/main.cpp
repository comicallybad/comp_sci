#include <iostream>
using namespace std;

int main()
{
    int x = 2, y = 5, z = 8, k = 0;

    //if(expressoin)? then (expression) : else (expression)
    x < y ? y = k : z = k;

    // '0' == 48
    // '9' == 57
    // 'A' == 65
    // 'Z' == 90
    // 'a' == 97
    // 'z' == '122'
    // ' ' == 32
    // '.' == 46

    // 'Y' && 'y'

    // '1' < '2'

    // 'A' > 'a'  ==> 65 > 7

    return 0;
}